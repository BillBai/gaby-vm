# Generated by ios-runner/generate.sh (xcodegen). The committed sources of
# truth are project.yml (host) and the repo CMakeLists (library); the .xcodeproj
# is regenerated, never committed.
*.xcodeproj/

# Local, untracked signing config (DEVELOPMENT_TEAM for on-device runs).
Signing.xcconfig

# xcodebuild scratch from local on-device runs.
DerivedData/
.derived-data-log-*
