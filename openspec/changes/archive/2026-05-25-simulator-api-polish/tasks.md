## 1. Public API rename: MemoryWrite → MemoryWriteEvent

- [x] 1.1 In `include/gaby_vm/simulator.h`, rename the nested struct `gaby_vm::Simulator::MemoryWrite` to `MemoryWriteEvent` (declaration around line 173). Update the `MemoryWriteObserver` alias signature (around line 189) to `std::function<void(const MemoryWriteEvent&)>`. Update the docstring on `SetMemoryWriteObserver` and any prose in the surrounding `--- Memory-write observation ---` section that names the struct.
- [x] 1.2 In `src/gaby_vm/simulator.cc`, update `ForwardingWriteSink::Record` (around line 91-101) so it constructs `Simulator::MemoryWriteEvent{addr, size, value_lo, value_hi}`. The `observer` field's type `Simulator::MemoryWriteObserver` is unchanged (the alias now points at the renamed struct).
- [x] 1.3 In `src/gaby_vm/shadow_runner.cc`, update all eight references currently spelling `Simulator::MemoryWrite`: the two `SetMemoryWriteObserver` lambdas (lines 70 and 72), the four `Simulator::MemoryWrite` mentions in `CompareMemoryWrites` (lines 226-229), and the two `std::vector<Simulator::MemoryWrite>` storage members (lines 271-272). Leave `DivergenceReport::Kind::MemoryWrite` unchanged — it is a separate enum naming a *kind of divergence*, not the event payload.
- [x] 1.4 In `test/reentrancy_test.cc`, update the `SetMemoryWriteObserver` lambda parameter at line 139 from `const gaby_vm::Simulator::MemoryWrite&` to `const gaby_vm::Simulator::MemoryWriteEvent&`.
- [x] 1.5 Sweep: `git grep -nE 'Simulator::MemoryWrite\b' include src test` returns no hits. The only remaining `MemoryWrite` mentions are `DivergenceReport::Kind::MemoryWrite` (shadow_runner.h:41, shadow_runner.cc:232), the imported `vixl::aarch64::MemoryWriteSink` (src/aarch64/simulator-aarch64.h), and prose mentions of "memory write" / "memory-write" in docstrings.

## 2. Minimum stack-size assertion (`kMinStackSize`)

- [x] 2.1 In `include/gaby_vm/simulator.h`, add a public `static constexpr size_t kMinStackSize = 12 * 1024;` to `gaby_vm::Simulator`. Place it just above the constructor declaration so the documentation reads top-to-bottom. Document it briefly: the value matches `vixl::aarch64::SimStack::limit_guard_size_ + usable_size_` defaults in `src/aarch64/simulator-aarch64.h`; the constructor rejects below-minimum buffers via `VIXL_ABORT_WITH_MSG`. Update the existing `stack_size` docstring on the constructor (currently lines 47-48) to point at `kMinStackSize`.
- [x] 2.2 In `src/gaby_vm/simulator.cc`, add a `static_assert(Simulator::kMinStackSize >= vixl::aarch64::SimStack().GetLimitGuardSize() + ...)` near the top — or, more practically, a `static_assert(Simulator::kMinStackSize >= 12 * 1024, "..."` with a comment cross-referencing the VIXL fields. Pick whichever the headers permit at compile time. The intent is that future VIXL imports that bump the SimStack defaults trip the assertion.
- [x] 2.3 In `src/gaby_vm/simulator.cc`, add the validation in the constructor body before the `std::make_unique<Impl>(...)` call. Two options match the file's style: (a) route the constructor through a free helper `ValidateStackSize(size_t)` in the anonymous namespace; or (b) inline the `VIXL_ABORT_WITH_MSG` in the constructor body via a small refactor to a delegating constructor. Either way, the abort path must not allocate `Impl`. Use the existing accessor-abort pattern (`std::snprintf` into a 160-byte stack buffer, then `VIXL_ABORT_WITH_MSG`) so the message names both the rejected `stack_size` and `kMinStackSize`.
- [x] 2.4 Add a new abort/death test file `test/simulator_constructor_stack_test.cc`, modeled on `test/typed_register_io_abort_test.cc`. Cover three rejection cases (`stack_size = 0`, `stack_size = 16`, `stack_size = Simulator::kMinStackSize - 1`) and one success case (`stack_size = Simulator::kMinStackSize` constructs and `Read(GpRegister::SP)` returns the expected 16-byte-aligned top). Register the binary in the corresponding `CMakeLists.txt` alongside the other test executables.
- [x] 2.5 Confirm `ctest` (from `build/`) passes the full existing suite. No existing test should require adjustment — every test's `StackBuffer` is 16 KiB or larger.

## 3. `SetCPUFeatures(All())` TODO comment

- [x] 3.1 In `src/gaby_vm/simulator.cc` (currently around lines 145-147), replace the existing one-line comment above `vsim.SetCPUFeatures(vixl::CPUFeatures::All());` with a `// TODO(simulator-cpu-features):` block recording (a) why `All()` is used during V1 bring-up — the debug-track auditor and the cache-track pre-screen share one feature set, `All()` keeps either from rejecting baseline instructions; (b) why the eventual target is a narrow EL0 user-mode subset — `AGENTS.md` «Scope», plus the cache-track pre-screen waste argument; (c) the deferred follow-up belongs in its own change. Do not modify the call itself.
- [x] 3.2 Sweep: `git grep -n 'TODO(simulator-cpu-features)' src/` returns exactly one hit (the comment above the SetCPUFeatures call). The kebab-case tag is the lookup key for the follow-up change.

## 4. Verification

- [x] 4.1 Build: `cmake --build build` (or the project's standard build command) succeeds, no new warnings.
- [x] 4.2 Tests: `ctest` (or platform equivalent) passes the full suite, including the new `simulator_constructor_stack_test`.
- [x] 4.3 Public-header reread: re-read `include/gaby_vm/simulator.h` end-to-end. The `--- Memory-write observation ---` section uses `MemoryWriteEvent` consistently; `kMinStackSize` is documented right above the constructor and the constructor docstring points at it.
- [x] 4.4 OpenSpec self-check: `openspec status --change simulator-api-polish` reports the change applyRequires-complete; `openspec validate simulator-api-polish` (if available in this project's tooling) passes.
